version: '3.8'
services:
  postgres:
    image: postgres:15
    container_name: batch_postgres
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
      POSTGRES_DB: reconciliation_db
    ports:
      - "5433:5432"